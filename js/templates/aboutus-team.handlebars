 {{#each data}}           
         <div class="col-md-3">
                <a href="#" onclick="loadData('team/{{id}}','#content','aboutus-details')">
                    <img class="img-rounded" src="{{thumbpath}}" alt="">
                    <p class="text-dark font-weight-bold pt-3">{{name}}</p>
                </a>               
                <p>{{summary}}</p>
         </div>
         {{/each}}